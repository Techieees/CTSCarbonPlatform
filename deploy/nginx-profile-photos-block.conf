# Deny direct access to legacy public profile photo URLs. Photos are served only via
# Flask /api/profile-photo/<user_id> (login_required). Merge this inside the same server { }
# block as your location /static handler, or include after your main static location.
location ^~ /static/profile_photos/ {
  deny all;
  return 404;
}
