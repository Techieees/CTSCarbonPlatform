# CTS Carbon Platform — HTTP body size for uploads (Evidence, multipart).
# Include this file inside the server { } block that proxies to the Flask app
# (same context as your existing proxy_pass to gunicorn). Example:
#   server { ... include /path/to/nginx-carbon-platform.conf; ... }
client_max_body_size 50M;
